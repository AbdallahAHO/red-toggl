'use strict';

module.exports = function(dir, otherDirs, program){
  console.log(
    `Running command mkdir ... \n` +
    `Arguments received: \n` +
    `dir: ${dir} \n` +
    `otherDirs: ${otherDirs} \n`
  );
}
